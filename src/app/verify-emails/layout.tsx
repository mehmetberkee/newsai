export const metadata = {
  title: "Verify Email - News App",
  description: "Verify your email address",
};

export default function VerifyEmailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
